#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define pb push_back

/*
    f(a, b, n): # paths from a to b not touching 0
    on the way
    g(a, b, n): # paths from a to b, no restrictions
*/

ll f[10][10][40];
ll g[10][10][40];

int main() {
    
    return 0;
}